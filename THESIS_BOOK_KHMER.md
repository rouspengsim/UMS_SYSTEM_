# សៀវភៅសារណា

## ប្រធានបទ

# ប្រព័ន្ធគ្រប់គ្រងសាលារៀនផ្ទាល់ខ្លួន

**Personal School Management System**

រៀបរៀងដោយ៖ `........................................`

ជំនាញ៖ `........................................`

គ្រឹះស្ថានសិក្សា៖ `........................................`

សាស្ត្រាចារ្យណែនាំ៖ `........................................`

ឆ្នាំសិក្សា៖ `2025 - 2026`

---

## សេចក្តីថ្លែងអំណរគុណ

ខ្ញុំសូមថ្លែងអំណរគុណយ៉ាងជ្រាលជ្រៅចំពោះសាស្ត្រាចារ្យណែនាំ មាតាបិតា មិត្តរួមថ្នាក់ និងអ្នកពាក់ព័ន្ធទាំងអស់ ដែលបានផ្តល់ការណែនាំ កម្លាំងចិត្ត និងជំនួយក្នុងការស្រាវជ្រាវ និងអភិវឌ្ឍប្រព័ន្ធនេះ។ ការគាំទ្រទាំងនេះបានធ្វើឲ្យការសិក្សា ការរចនា និងការអភិវឌ្ឍប្រព័ន្ធគ្រប់គ្រងសាលារៀនអាចសម្រេចបានដោយជោគជ័យ។

---

## សេចក្តីសង្ខេប

សារណានេះមានគោលបំណងសិក្សា រចនា និងអភិវឌ្ឍប្រព័ន្ធគ្រប់គ្រងសាលារៀនផ្ទាល់ខ្លួន ដែលអាចជួយគ្រប់គ្រងទិន្នន័យសិស្ស គ្រូ ថ្នាក់រៀន មុខវិជ្ជា វត្តមាន ពិន្ទុ កាលវិភាគ ការទូទាត់ សេចក្តីជូនដំណឹង របាយការណ៍ និងវិញ្ញាបនបត្រ។ ប្រព័ន្ធនេះត្រូវបានបង្កើតដោយប្រើ React, TypeScript, TanStack Router, TanStack Query, Tailwind CSS និង Supabase។

ប្រព័ន្ធមានការចូលប្រើតាមតួនាទីអ្នកប្រើចំនួនបី គឺ admin, teacher និង student។ Admin អាចគ្រប់គ្រងប្រព័ន្ធទាំងមូល។ Teacher អាចមើល និងគ្រប់គ្រងទិន្នន័យដែលពាក់ព័ន្ធនឹងថ្នាក់ និងមុខវិជ្ជាដែលខ្លួនបង្រៀន។ Student អាចមើលព័ត៌មានផ្ទាល់ខ្លួន វត្តមាន ពិន្ទុ កាលវិភាគ ការទូទាត់ សេចក្តីជូនដំណឹង និងវិញ្ញាបនបត្រ។

លទ្ធផលនៃការអភិវឌ្ឍបង្ហាញថា ប្រព័ន្ធអាចកាត់បន្ថយការងារដោយដៃ បង្កើនភាពត្រឹមត្រូវនៃទិន្នន័យ និងធ្វើឲ្យការគ្រប់គ្រងព័ត៌មានសាលាមានប្រសិទ្ធភាពជាងមុន។

---

## មាតិកា

- ជំពូកទី 1៖ សេចក្តីផ្តើម
- ជំពូកទី 2៖ ការសិក្សាប្រព័ន្ធបច្ចុប្បន្ន និងតម្រូវការ
- ជំពូកទី 3៖ ការវិភាគ និងរចនាប្រព័ន្ធ
- ជំពូកទី 4៖ ការអភិវឌ្ឍ និងពន្យល់ Source Code
- ជំពូកទី 5៖ ការធ្វើតេស្ត និងលទ្ធផល
- ជំពូកទី 6៖ សេចក្តីសន្និដ្ឋាន និងអនុសាសន៍
- ឯកសារយោង
- ឧបសម្ព័ន្ធ

---

# ជំពូកទី 1៖ សេចក្តីផ្តើម

## 1.1 ផ្ទៃខាងក្រោយ

ក្នុងស្ថាប័នអប់រំ ការគ្រប់គ្រងទិន្នន័យសិស្ស គ្រូ ថ្នាក់រៀន វត្តមាន ពិន្ទុ និងការទូទាត់ ជាការងារសំខាន់ដែលត្រូវការភាពត្រឹមត្រូវ និងល្បឿន។ ប្រសិនបើប្រើការកត់ត្រាដោយដៃ ឬឯកសារ spreadsheet ច្រើន វាអាចបង្កើតបញ្ហាដូចជា data duplication, data loss, ការស្វែងរកយឺត និងការរៀបចំរបាយការណ៍មិនទាន់ពេល។

ដូច្នេះ ប្រព័ន្ធគ្រប់គ្រងសាលារៀនតាម web application ត្រូវបានបង្កើតឡើង ដើម្បីជួយឲ្យការគ្រប់គ្រងទិន្នន័យសិក្សាមានភាពងាយស្រួល ត្រឹមត្រូវ និងសុវត្ថិភាព។

## 1.2 បញ្ហាដែលត្រូវដោះស្រាយ

បញ្ហាសំខាន់ៗដែលប្រព័ន្ធនេះចង់ដោះស្រាយមាន៖

- ការគ្រប់គ្រងសិស្ស និងគ្រូដោយដៃមានការលំបាក។
- ការកត់ត្រាវត្តមាន និងពិន្ទុអាចមានកំហុស ឬស្ទួន។
- ការរៀបចំកាលវិភាគអាចមានការជាន់ម៉ោងរវាងគ្រូ បន្ទប់ និងថ្នាក់។
- ការគ្រប់គ្រងការទូទាត់ និងបង្កាន់ដៃត្រូវការប្រព័ន្ធត្រឹមត្រូវ។
- ការផ្ញើសេចក្តីជូនដំណឹងទៅសិស្ស និងគ្រូមិនទាន់មានមធ្យោបាយកណ្តាល។
- ការបង្កើតរបាយការណ៍ និងវិញ្ញាបនបត្រត្រូវការពេលវេលាច្រើន។

## 1.3 គោលបំណង

គោលបំណងរបស់ប្រព័ន្ធមាន៖

- បង្កើតប្រព័ន្ធគ្រប់គ្រងសាលារៀនតាម web application។
- គាំទ្រ user roles ចំនួន 3 គឺ admin, teacher និង student។
- គ្រប់គ្រងសិស្ស គ្រូ ថ្នាក់ មុខវិជ្ជា និងបន្ទប់រៀន។
- កត់ត្រាវត្តមាន និងពិន្ទុសិស្ស។
- រៀបចំកាលវិភាគដោយមានការពិនិត្យ conflict។
- គ្រប់គ្រងការទូទាត់ និងបោះពុម្ព receipt។
- ផ្ញើសេចក្តីជូនដំណឹង និងបោះពុម្ពរបាយការណ៍។
- គាំទ្រភាសាខ្មែរ និងអង់គ្លេស។

## 1.4 វិសាលភាព

ប្រព័ន្ធនេះផ្តោតលើការគ្រប់គ្រងស្ថាប័នអប់រំខ្នាតតូចដល់មធ្យម។ វាមាន modules សំខាន់ៗដូចជា៖

- Authentication និង authorization
- Dashboard
- Students
- Teachers
- Classes
- Classrooms
- Subjects
- Attendance
- Exams and scores
- Timetable
- Payments
- Notifications
- Reports
- Roles
- Certificates

## 1.5 បច្ចេកវិទ្យាដែលប្រើ

| ផ្នែក | បច្ចេកវិទ្យា |
|---|---|
| Frontend | React 19, TypeScript |
| Routing | TanStack Router / TanStack Start |
| Data Fetching | TanStack Query |
| Backend/Database | Supabase, PostgreSQL |
| Authentication | Supabase Auth |
| Security | Row Level Security |
| Styling | Tailwind CSS, Radix UI |
| Charts | Recharts |
| Icons | Lucide React |
| Build Tool | Vite |

---

# ជំពូកទី 2៖ ការសិក្សាប្រព័ន្ធបច្ចុប្បន្ន និងតម្រូវការ

## 2.1 ការសិក្សាប្រព័ន្ធបច្ចុប្បន្ន

ប្រព័ន្ធបច្ចុប្បន្ននៅសាលាខ្លះអាចនៅតែប្រើការកត់ត្រាទិន្នន័យដោយដៃ ឬប្រើឯកសារ Excel ដាច់ដោយឡែក។ វាមានបញ្ហាដូចជា៖

- ពិបាកស្វែងរកព័ត៌មានសិស្ស ឬគ្រូ។
- ពិបាកដឹងថា class មួយមានសិស្សប៉ុន្មាននាក់។
- ពិបាកបង្កើត report ឲ្យបានលឿន។
- ទិន្នន័យអាចស្ទួន ឬបាត់បង់។
- មិនមាន role-based access control ច្បាស់លាស់។

## 2.2 តម្រូវការមុខងារ

តម្រូវការមុខងារសំខាន់ៗមាន៖

- User អាច login តាម role។
- Admin អាចបង្កើត និងកែប្រែ student/teacher account។
- Admin អាចគ្រប់គ្រង classes, rooms, subjects និង timetable។
- Teacher អាចកត់ attendance និងបញ្ចូល scores សម្រាប់ class ដែលខ្លួនបង្រៀន។
- Student អាចមើល profile, attendance, scores, timetable, payments និង certificates។
- ប្រព័ន្ធអាចបង្កើត invoice និង receipt។
- ប្រព័ន្ធអាចផ្ញើ notifications តាម role។
- ប្រព័ន្ធអាចបោះពុម្ព reports និង certificates។

## 2.3 តម្រូវការមិនមែនមុខងារ

- Security៖ ទិន្នន័យត្រូវការពារដោយ authentication, authorization និង RLS។
- Usability៖ UI ត្រូវងាយប្រើ និងគាំទ្រភាសាខ្មែរ។
- Performance៖ Dashboard និង tables គួរតែ load លឿន។
- Maintainability៖ Code ត្រូវបំបែកជា modules និង reusable components។
- Reliability៖ Database constraints ត្រូវការពារទិន្នន័យស្ទួន ឬមិនត្រឹមត្រូវ។

---

# ជំពូកទី 3៖ ការវិភាគ និងរចនាប្រព័ន្ធ

## 3.1 ស្ថាបត្យកម្មប្រព័ន្ធ

ប្រព័ន្ធនេះប្រើ client-server architecture។ Frontend ត្រូវបានអភិវឌ្ឍដោយ React និង TypeScript។ Frontend ទាក់ទងទៅ Supabase ដើម្បីធ្វើ authentication និង CRUD operations។ Operation ដែលត្រូវការសិទ្ធិខ្ពស់ ដូចជា create account ឬ update payment status ត្រូវធ្វើតាម server function។

លំហូរទូទៅ៖

1. User បើក browser ហើយចូល login page។
2. ប្រព័ន្ធផ្ញើ credential ទៅ Supabase Auth។
3. បន្ទាប់ពី login ជោគជ័យ ប្រព័ន្ធទាញ role ពី `user_roles`។
4. Route guard ពិនិត្យថា user អាចចូល page នោះឬអត់។
5. Page នីមួយៗទាញ data ពី Supabase តាម role និង RLS policies។

## 3.2 User Roles

| Role | សិទ្ធិប្រើប្រាស់ |
|---|---|
| Admin | គ្រប់គ្រងប្រព័ន្ធទាំងមូល |
| Teacher | គ្រប់គ្រង class, attendance និង scores ដែលខ្លួនបង្រៀន |
| Student | មើលព័ត៌មានផ្ទាល់ខ្លួន និងទិន្នន័យសិក្សារបស់ខ្លួន |

## 3.3 Database Design

Tables សំខាន់ៗមាន៖

| Table | គោលបំណង |
|---|---|
| `profiles` | ព័ត៌មានមូលដ្ឋានរបស់ user |
| `user_roles` | កំណត់ role របស់ user |
| `students` | ទិន្នន័យសិស្ស |
| `teachers` | ទិន្នន័យគ្រូ |
| `classes` | ទិន្នន័យថ្នាក់ |
| `enrollments` | ទំនាក់ទំនងសិស្សនិងថ្នាក់ |
| `attendance` | វត្តមានសិស្ស |
| `teacher_attendance` | វត្តមានគ្រូ |
| `subjects` | មុខវិជ្ជា |
| `subject_scores` | ពិន្ទុតាមមុខវិជ្ជា |
| `timetable_slots` | កាលវិភាគ |
| `payments` | ការទូទាត់ |
| `notifications` | សេចក្តីជូនដំណឹង |
| `certificates` | វិញ្ញាបនបត្រ |

## 3.4 Diagram Assets

Project មាន diagram សម្រាប់ដាក់ក្នុង thesis រួចហើយ៖

- `supabase/er-diagram.svg`
- `supabase/use-case-diagram.svg`
- `supabase/context-diagram.svg`
- `supabase/dfd-level-0.svg`
- `supabase/process-diagram.svg`
- `supabase/database-schema.svg`

---

# ជំពូកទី 4៖ ការអភិវឌ្ឍ និងពន្យល់ Source Code

## 4.1 Root Application

File: `src/routes/__root.tsx`

```tsx
<QueryClientProvider client={queryClient}>
  <ThemeProvider>
    <I18nProvider>
      <AuthProvider>
        <Outlet />
        <Toaster position="top-right" richColors closeButton />
      </AuthProvider>
    </I18nProvider>
  </ThemeProvider>
</QueryClientProvider>
```

ការពន្យល់៖

Root application ដាក់ providers សំខាន់ៗសម្រាប់ app ទាំងមូល។ `QueryClientProvider` គ្រប់គ្រង data fetching cache។ `ThemeProvider` គ្រប់គ្រង dark/light mode។ `I18nProvider` គ្រប់គ្រងភាសា។ `AuthProvider` គ្រប់គ្រង user session និង roles។

## 4.2 Authentication

File: `src/lib/auth.tsx`

```tsx
const ROLE_PRIORITY: Role[] = ["admin", "teacher", "student"];

const primaryRole =
  (requestedRole && roles.includes(requestedRole) ? requestedRole : null) ??
  ROLE_PRIORITY.find((r) => roles.includes(r)) ??
  null;
```

ការពន្យល់៖

Code នេះជ្រើស role សំខាន់សម្រាប់ user។ ប្រសិនបើ user មាន role ច្រើន ប្រព័ន្ធជ្រើសតាម priority គឺ admin មុន teacher ហើយ student ចុងក្រោយ។

## 4.3 Protected Routes

File: `src/routes/app.tsx`

```tsx
if (!loading && !user) {
  navigate({ to: "/" });
}

if (!loading && user && primaryRole && !canAccessPath(primaryRole, pathname)) {
  navigate({ to: "/app" });
}
```

ការពន្យល់៖

Protected route ការពារមិនឲ្យ user ដែលមិនបាន login ចូល app។ វាក៏ពិនិត្យថា role បច្ចុប្បន្នអាចចូល path នោះឬអត់។

## 4.4 Dashboard Page

File: `src/routes/app.index.tsx`

```tsx
const [students, teachers, classes, payments] = await Promise.all([
  supabase.from("students").select("id", { count: "exact", head: true }),
  supabase.from("teachers").select("id", { count: "exact", head: true }),
  supabase.from("classes").select("id", { count: "exact", head: true }),
  supabase.from("payments").select("amount,status,paid_date,created_at"),
]);

const paidRows = (payments.data ?? []).filter((p) => p.status === "paid");
const totalRevenue = paidRows.reduce((sum, r) => sum + Number(r.amount ?? 0), 0);
```

ការពន្យល់៖

Dashboard ទាញចំនួនសិស្ស គ្រូ ថ្នាក់ និង payment records ដើម្បីបង្ហាញ summary cards និង charts។ `Promise.all` ធ្វើឲ្យ query ច្រើនរត់ពេលតែមួយ។

## 4.5 Students Page

File: `src/routes/app.students.tsx`

```tsx
if (primaryRole === "teacher") {
  const scope = await findTeacherClassScope(user);
  const classNames = scope?.classNames ?? [];
  if (classNames.length === 0) return [];

  const { data, error } = await supabase
    .from("students")
    .select("*")
    .in("class_name", classNames)
    .order("created_at", { ascending: false });
  if (error) throw error;
  return (data ?? []) as StudentRow[];
}
```

ការពន្យល់៖

Teacher មើលតែសិស្សក្នុង class ដែលខ្លួនបង្រៀន។ Function `findTeacherClassScope` ស្វែងរក class scope របស់ teacher ហើយ query students តាម `class_name`។

## 4.6 Attendance Page

File: `src/routes/app.attendance.tsx`

```tsx
const { error } = await supabase.from("attendance").upsert(
  {
    student_id: sid,
    class_id: classId,
    date: recordDate,
    semester,
    week_number: week,
    day_of_week: day,
    subject_code: subjectCode,
    status,
  },
  { onConflict: "student_id,class_id,semester,week_number,day_of_week,subject_code" },
);
```

ការពន្យល់៖

Attendance ប្រើ `upsert` ដើម្បី insert ឬ update record តាម unique key។ វាជួយការពារមិនឲ្យមាន attendance ស្ទួនសម្រាប់សិស្សម្នាក់ក្នុងថ្ងៃ និង subject ដូចគ្នា។

## 4.7 Exams and Scores Page

File: `src/routes/app.exams.tsx`

```tsx
const total = totalSubjectScore({
  attendance,
  assignment,
  midterm,
  final,
});

const payload = {
  student_id: studentId,
  class_id: classId,
  semester,
  week_number: weekNumber,
  subject_code: subject,
  attendance_score: attendanceScore,
  assignment_score: nextAssignment,
  midterm_score: nextMidterm,
  final_score: nextFinal,
  score: total,
};
```

ការពន្យល់៖

Scores ត្រូវបានបំបែកជា attendance, assignment, midterm និង final។ ប្រព័ន្ធគណនាពិន្ទុសរុប ហើយរក្សាទុកក្នុង table `subject_scores`។

## 4.8 Timetable Page

File: `src/routes/app.timetable.tsx`

```ts
function timeOverlaps(startA: string, endA: string, startB: string, endB: string) {
  return startA < endB && startB < endA;
}
```

ការពន្យល់៖

Function នេះពិនិត្យម៉ោងជាន់គ្នា។ វាត្រូវបានប្រើដើម្បីរក conflict រវាង teacher ឬ room នៅក្នុង timetable។

## 4.9 Payments Page

File: `src/routes/app.payments.tsx`

```tsx
const stats = useMemo(() => {
  const paid = payments
    .filter((p) => p.status === "paid")
    .reduce((s, p) => s + Number(p.amount), 0);
  const pending = payments
    .filter((p) => p.status === "pending")
    .reduce((s, p) => s + Number(p.amount), 0);
  return { paid, pending };
}, [payments]);
```

ការពន្យល់៖

Payments page គណនាចំនួនប្រាក់ paid និង pending ដើម្បីបង្ហាញ summary។ `useMemo` ជួយកាត់បន្ថយការគណនាឡើងវិញពេល data មិនផ្លាស់ប្តូរ។

## 4.10 Notifications Page

File: `src/routes/app.notifications.tsx`

```tsx
const richPayload = {
  title: f.title,
  body: f.body || null,
  kind: f.kind,
  target_user_id: null,
  target_role: targetRole,
  media_url: mediaUrl,
  media_type: uploadedMediaType,
  created_by: session.user.id,
};

const { error } = await supabase.from("notifications").insert(richPayload);
```

ការពន្យល់៖

Notification អាចផ្ញើទៅ role ណាមួយ ឬ user ជាក់លាក់។ Payload រួមមាន title, body, kind, target role, media និង user ដែលបានបង្កើត។

## 4.11 Reports Page

File: `src/routes/app.reports.tsx`

```tsx
const totalAtt = (attendance.data ?? []).length;
const present = (attendance.data ?? []).filter(
  (a) => a.status === "present" || a.status === "late",
).length;
const rate = totalAtt > 0 ? Math.round((present / totalAtt) * 100) : 0;
```

ការពន្យល់៖

Reports page គណនា attendance rate ដោយយក present និង late ជាការមានវត្តមាន។ វាប្រើសម្រាប់បង្ហាញស្ថិតិរួមក្នុង management report។

## 4.12 Certificates Page

File: `src/routes/app.certificates.tsx`

```tsx
const { error } = await supabase
  .from("certificates")
  .insert({ student_id: f.student_id, title: f.title, kind: f.kind });
```

ការពន្យល់៖

Admin អាចចេញ certificate សម្រាប់សិស្ស។ Record ថ្មីត្រូវបាន insert ទៅ table `certificates` ហើយអាច print ជាឯកសារផ្លូវការ។

---

# ជំពូកទី 5៖ ការធ្វើតេស្ត និងលទ្ធផល

## 5.1 វិធីសាស្ត្រធ្វើតេស្ត

ការធ្វើតេស្តត្រូវបានអនុវត្តលើមុខងារសំខាន់ៗ៖

- Login ជា admin, teacher និង student។
- Role-based navigation និង route guard។
- CRUD សិស្ស គ្រូ ថ្នាក់ និងមុខវិជ្ជា។
- កត់ attendance និងបញ្ចូល scores។
- បង្កើត timetable និងពិនិត្យ conflict។
- បង្កើត invoice និង mark payment as paid។
- ផ្ញើ notification។
- បោះពុម្ព reports និង certificates។

## 5.2 Test Cases

| ល.រ | មុខងារ | Input | Expected Result | Status |
|---|---|---|---|---|
| 1 | Admin login | Email/password ត្រឹមត្រូវ | ចូល Dashboard | Pass |
| 2 | Student login | Student ID/password | ចូល Student Portal | Pass |
| 3 | Teacher access students | Teacher account | មើលតែសិស្សក្នុង class ខ្លួន | Pass |
| 4 | Attendance upsert | Status P/A/L/E | Record ត្រូវបានរក្សាទុក | Pass |
| 5 | Score validation | ពិន្ទុលើស max | បង្ហាញ error | Pass |
| 6 | Timetable conflict | Teacher ជាន់ម៉ោង | បង្ហាញ conflict message | Pass |
| 7 | Payment paid | Mark paid | Status ក្លាយជា paid និង print receipt | Pass |
| 8 | Certificate issue | Student + title | Certificate ត្រូវបានបង្កើត | Pass |

## 5.3 លទ្ធផល

ប្រព័ន្ធអាចដំណើរការមុខងារសំខាន់ៗបានតាមតម្រូវការ។ Dashboard អាចបង្ហាញស្ថិតិរួម។ Admin អាចគ្រប់គ្រងទិន្នន័យសាលា។ Teacher អាចគ្រប់គ្រងវត្តមាន និងពិន្ទុតាម class របស់ខ្លួន។ Student អាចមើលព័ត៌មានផ្ទាល់ខ្លួន និងទិន្នន័យសិក្សា។

---

# ជំពូកទី 6៖ សេចក្តីសន្និដ្ឋាន និងអនុសាសន៍

## 6.1 សេចក្តីសន្និដ្ឋាន

ប្រព័ន្ធគ្រប់គ្រងសាលារៀនផ្ទាល់ខ្លួននេះបានបំពេញតម្រូវការសំខាន់ៗសម្រាប់ការគ្រប់គ្រងទិន្នន័យសិស្ស គ្រូ ថ្នាក់ វត្តមាន ពិន្ទុ កាលវិភាគ ការទូទាត់ សេចក្តីជូនដំណឹង របាយការណ៍ និងវិញ្ញាបនបត្រ។ ប្រព័ន្ធប្រើ role-based access control និង Supabase RLS ដើម្បីការពារទិន្នន័យ។ ការប្រើ React និង TypeScript ធ្វើឲ្យ frontend មានរចនាសម្ព័ន្ធច្បាស់ និងងាយថែទាំ។

## 6.2 អត្ថប្រយោជន៍

- កាត់បន្ថយការងារដោយដៃ។
- បង្កើនភាពត្រឹមត្រូវនៃទិន្នន័យ។
- ធ្វើឲ្យការស្វែងរក និងរបាយការណ៍លឿនជាងមុន។
- ផ្តល់ portal ដាច់ដោយឡែកសម្រាប់ admin, teacher និង student។
- គាំទ្រភាសាខ្មែរ ដែលសមស្របសម្រាប់បរិបទកម្ពុជា។

## 6.3 អនុសាសន៍សម្រាប់អនាគត

អាចបន្ថែមមុខងារបន្ថែមដូចជា៖

- Mobile app សម្រាប់ Android និង iOS។
- Real-time notification ដោយ push notification។
- Integration ជាមួយ payment gateway។
- Export reports ជា PDF/Excel។
- Parent portal សម្រាប់ឪពុកម្តាយ។
- Advanced analytics សម្រាប់លទ្ធផលសិក្សា។

---

# ឯកសារយោង

- React Documentation: `https://react.dev`
- TypeScript Documentation: `https://www.typescriptlang.org`
- TanStack Router Documentation: `https://tanstack.com/router`
- TanStack Query Documentation: `https://tanstack.com/query`
- Supabase Documentation: `https://supabase.com/docs`
- Tailwind CSS Documentation: `https://tailwindcss.com`

---

# ឧបសម្ព័ន្ធ

## Appendix A: Source Code Explanation Files

ឯកសារពន្យល់ source code បន្ថែមក្នុង project៖

- `THESIS_KHMER_SOURCE_CODE_EXPLANATION.md`
- `SOURCE_CODE_PART_BY_PART_KHMER.md`
- `PAGE_SOURCE_CODE_EXPLANATION_KHMER.md`

## Appendix B: Diagram Files

- `supabase/er-diagram.svg`
- `supabase/use-case-diagram.svg`
- `supabase/context-diagram.svg`
- `supabase/context-flow-diagram.svg`
- `supabase/dfd-level-0.svg`
- `supabase/process-diagram.svg`
- `supabase/database-schema.svg`

## Appendix C: Main Source Code Files

- `src/routes/__root.tsx`
- `src/routes/app.tsx`
- `src/routes/app.index.tsx`
- `src/routes/app.students.tsx`
- `src/routes/app.teachers.tsx`
- `src/routes/app.classes.tsx`
- `src/routes/app.attendance.tsx`
- `src/routes/app.exams.tsx`
- `src/routes/app.timetable.tsx`
- `src/routes/app.payments.tsx`
- `src/routes/app.notifications.tsx`
- `src/routes/app.reports.tsx`
- `src/routes/app.roles.tsx`
- `src/routes/app.certificates.tsx`
- `src/lib/auth.tsx`
- `src/lib/role-access.ts`
- `src/integrations/supabase/client.ts`

